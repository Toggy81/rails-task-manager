<h1><%= @task.title %></h1>
<%= link_to 'Edit a Task', edit_path %>
<p><%= @task.details %></p>
<p><%= @task.completed == true ? "Completed" : "Not done yet" %></p>
<%= link_to 'Back to list', tasks_path %>
